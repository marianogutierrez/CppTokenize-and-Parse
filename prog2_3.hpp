//
// Created by Mariano Gutierrez on 3/14/18.
//

#include <vector>
#include <string>
using namespace std;

class Parser {
private:
    // the void

public:
    Parser();
    ~Parser();
    bool Parse(vector<string> myVec);
};
